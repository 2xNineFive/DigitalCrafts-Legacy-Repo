// Define at least five key/value pairs in an object that describes a rubix cube. Be sure that you cover all primitive data types (i.e. string, number, and boolean) as well as an array.
// Log to the console the keys you defined to hold a string and an array.

// string, check 
// number, check
// boolean, check
// array, check


const rubixCube = {
    // 1. string
    coolFact : 'As of January 2009, 350 million cubes have been sold world wide.',
    
    // 2. number
    NumFaces : 6,
    
    // 3. number
    cubeSize : (3 * 3 * 6),
    
    // 4. boolean
    isHard : true, 

    // 5. array
    colors : ['white', 'yellow', 'red', 'blue', 'orange', 'green'],
};

console.log(rubixCube.coolFact);
// console.log(rubixCube.cubeSize);
console.log(rubixCube.colors);
